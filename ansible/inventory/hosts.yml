all:
  children:
    home:
      hosts:
        artemis:
          ansible_host: 192.168.5.185       # LAN IP or hostname of the artemis machine
          ansible_user: deploy
          ansible_ssh_private_key_file: /home/dagu/.ssh/id_ed25519
    vps:
      hosts:
        icarus:
          # ansible_host from ICARUS_ANSIBLE_HOST (Infisical prod) — see host_vars/icarus.yml
          ansible_user: deploy
          ansible_ssh_private_key_file: /home/dagu/.ssh/id_ed25519
        # Add future VPS hosts here:
        # zeus:
        #   ansible_host: x.x.x.x
        #   ansible_user: deploy
        #   ansible_ssh_private_key_file: /home/dagu/.ssh/id_ed25519
