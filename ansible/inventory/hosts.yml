all:
  children:
    home:
      hosts:
        artemis:
          ansible_host: CHANGEME       # LAN IP or hostname of the artemis machine
          ansible_user: deploy
          ansible_ssh_private_key_file: /root/.ssh/id_ed25519
    vps:
      hosts:
        hermes:
          ansible_host: CHANGEME       # DigitalOcean droplet IP or hostname
          ansible_user: deploy
          ansible_ssh_private_key_file: /root/.ssh/id_ed25519
        # Add future VPS hosts here:
        # zeus:
        #   ansible_host: x.x.x.x
        #   ansible_user: deploy
        #   ansible_ssh_private_key_file: /root/.ssh/id_ed25519
